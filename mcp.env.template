# =============================================================================
# mcp.env  —  MCP Server Configuration (TEMPLATE)
# =============================================================================
#
# THIS IS A TEMPLATE FILE — safe to commit to GitHub.
# Your REAL mcp.env with actual secrets lives ONLY in the location of the scripts:
#   /location/of/mcp.env
# That real file is listed in .gitignore and must NEVER be committed.
#
# HOW TO USE THIS TEMPLATE:
#   1. Copy this file to the location of the rest of the mcp scripts:
#        cp mcp.env.template mcp.env
#   2. Fill in each placeholder with your real values
#   3. Keep mcp.env private — never share or commit it
#
# HOW TO LOAD MANUALLY (for testing):
#   set -a; source /location/of/mcp.env; set +a
#
# HOW TO GENERATE A NEW MCP_AUTH_TOKEN:
#   python3 -c "import secrets; print(secrets.token_hex(32))"
# =============================================================================

MCP_HOST=0.0.0.0
MCP_PORT=XXXX
MCP_AUTH_TOKEN=your-generated-token-here
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/your-webhook-id/your-webhook-token
AZURE_TENANT_ID=your-tenant-id
AZURE_CLIENT_ID=your-client-id
AZURE_CLIENT_SECRET=your-client-secret
AZURE_WORKSPACE_ID=your-workspace-id
JIRA_URL=https://yourcompany.atlassian.net
JIRA_EMAIL=your-email@example.com
JIRA_TOKEN=your-jira-api-token
JIRA_PROJECT_KEY=XXX
ANTHROPIC_API_KEY=sk-ant-your-key-here
# CLAUDE_MODEL=claude-sonnet-4-6